Code compilé (npm run build) sans erreurs.

Feature testée sur Android Chrome (cas nominal + erreurs).

Offline OK (si la feature doit marcher offline).​

Aucune erreur console bloquante.

UI minimale acceptable (pas d’alert() si évitable, messages clairs).

Données persistantes (IndexedDB) et migration DB testée.
