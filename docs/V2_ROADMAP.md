# V2 Roadmap

## MVP V2 (première livraison)
- Scan EAN-13 (natif BarcodeDetector si dispo + fallback polyfill) -> pré-remplit AddItem avec `barcode`.
- Réglages “durées par défaut” par catégorie (persistants).
- Export + import JSON (foyer sans backend) + partage Android si possible.

## Notes de scope
- Pas de backend V2: pas de comptes, pas de synchronisation temps réel.
- PWA Android prioritaire.
